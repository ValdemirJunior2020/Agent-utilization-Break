[build]
  command = "npx vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10.8.2"
  NPM_FLAGS = "--include=dev --no-audit --no-fund"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200